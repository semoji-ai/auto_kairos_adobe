# 평면 컴프 전환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 씬마다 컴프를 만들지 않고 모든 씬의 레이어를 `Final` 컴프 하나에 이름 규칙과 시각 구간으로 놓아, 편집할 때 컴프를 오가지 않게 한다.

**Architecture:** 지금은 씬 컴프가 좌표계 역할을 한다(컴프 크기 = 씬 이미지 크기). 평면에서는 `backend/manifest.py`가 좌표를 1920×1080 컴프 공간으로 변환해 구워서 내보내고, jsx는 받은 숫자를 찍기만 한다. 씬마다 `S{번호}_가이드` 널이 그 씬의 모든 레이어를 묶어 카메라와 편집의 손잡이가 된다.

**Tech Stack:** Python 3.11 stdlib(백엔드), pytest, ExtendScript(.jsx, ES5), CEP 패널 순수 ES5.

## Global Constraints

- 백엔드는 Python 3.11 **stdlib만** 쓴다. 새 서드파티 의존성을 넣지 않는다(PIL은 이미 있는 선택적 의존이며 기존 용법만 유지).
- jsx와 패널 JS는 **순수 ES5**다. `let`·`const`·화살표 함수·템플릿 리터럴·`class`를 쓰지 않는다. `var`와 `function`만 쓴다.
- 한국어 문자열·주석에 **일본어 가나와 한자를 쓰지 않는다.** 순수 한글과 영어만 쓴다.
- 배율은 **세로 기준**이다: `f = 1080 / 씬이미지높이`. 위아래를 자르지 않는다.
- 좌표 계산은 **백엔드에서만** 한다. jsx는 `position`·`scale`을 계산하지 않는다.
- 배율을 가이드 널에 싣지 않는다. 널은 배율 100으로 두고 카메라만 그것을 움직인다.
- 대상 컴프는 항상 **`Final`**이다. 활성 컴프를 쓰지 않는다.
- 레이어 이름 접두사는 `S{번호}_`이며 번호 표기는 `timeline.comp_num`을 쓴다(정수 2자리 0채움, 소수는 점→밑줄).
- 씬 번호 비교에 `int()`를 쓰지 않는다 — 삽입 씬의 소수 번호(25.25)가 뭉개진다.

---

### Task 1: 매니페스트가 컴프 좌표를 굽는다

**Files:**
- Modify: `backend/manifest.py` (`_scene_layers` 35-83, `build_manifest` 110-230)
- Test: `tests/test_manifest_flat.py` (신규)

**Interfaces:**
- Consumes: `timeline.comp_num(scene_number) -> str`, `timeline.scene_timings(proj_dir, data) -> [(scene, start, duration)]`, `imagegen.load_element_specs(out_base, sid) -> list`(항목에 `name`·`z`·`bbox`·`removed`)
- Produces:
  - `manifest.fit_transform(sw, sh, comp_w=1920, comp_h=1080) -> (f, ox)` — 세로 기준 배율과 좌우 중앙 정렬 오프셋
  - 매니페스트 씬 엔트리에 `start`(float), `prefix`(str, 예 `"S12_"`), `bgFill`(bool), `fit`({f, ox, w, h}), `imageFit`({position, scale}) 추가
  - 매니페스트 레이어 엔트리에 `aeName`(str) 추가, **모든 레이어에 `position`·`scale`이 반드시 있다**
  - `skipFinal` 키는 더 이상 내보내지 않는다

**배경:** `_scene_layers`는 지금 씬 이미지 픽셀 좌표로 `position`(bbox 중심)·`scale`(bbox폭/PNG폭×100)·`foot`(bbox 하단 중앙)을 낸다. bbox가 없는 레이어는 좌표를 싣지 않고 jsx가 채움 스케일로 겹친다. 평면에서는 그 폴백이 통하지 않으므로 모든 레이어에 좌표를 실어야 한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_manifest_flat.py`를 만든다.

```python
import json
from pathlib import Path

import pytest

from backend import manifest

SID = "abc123"


def test_fit_16by9ish():
    """1792x1024(1.750)를 1920x1080에 세로 기준으로 — 좌우 15px씩 여백."""
    f, ox = manifest.fit_transform(1792, 1024)
    assert f == pytest.approx(1080 / 1024)
    assert ox == pytest.approx((1920 - 1792 * (1080 / 1024)) / 2)
    assert ox == pytest.approx(15.0, abs=0.1)


def test_fit_3by2():
    f, ox = manifest.fit_transform(1536, 1024)
    assert f == pytest.approx(1.0546875)
    assert ox == pytest.approx(150.0, abs=0.1)


def test_fit_exact_16by9():
    f, ox = manifest.fit_transform(1920, 1080)
    assert f == pytest.approx(1.0)
    assert ox == pytest.approx(0.0)


def test_fit_wider_than_16by9_overflows():
    """2.0 비율은 세로 기준이면 좌우로 넘친다 — ox가 음수."""
    f, ox = manifest.fit_transform(1000, 500)
    assert f == pytest.approx(2.16)
    assert ox < 0


def test_fit_never_crops_vertically():
    """어떤 입력이든 세로는 정확히 컴프 높이를 채운다."""
    for sw, sh in [(1792, 1024), (1536, 1024), (900, 600), (480, 641), (1000, 500)]:
        f, _ = manifest.fit_transform(sw, sh)
        assert sh * f == pytest.approx(1080)


def test_fit_bad_size_falls_back():
    assert manifest.fit_transform(0, 0) == (1.0, 0.0)
    assert manifest.fit_transform(100, -5) == (1.0, 0.0)


# ---- 매니페스트 전체 ----

def _project(tmp_path: Path):
    """씬 2개(1792x1024 이미지 + 레이어 2장)를 갖춘 프로젝트."""
    from PIL import Image
    proj = tmp_path / "p1"
    (proj / "storyboard").mkdir(parents=True)
    (proj / "layers").mkdir()
    scenes_json = {"scenes": []}
    for n, sid in ((1, "aaa111"), (2, "bbb222")):
        img = proj / "storyboard" / f"sb_{sid}.png"
        Image.new("RGBA", (1792, 1024), (10, 10, 10, 255)).save(img)
        Image.new("RGBA", (1792, 1024), (20, 20, 20, 255)).save(proj / "layers" / f"{sid}__bg.png")
        Image.new("RGBA", (200, 400), (0, 0, 0, 255)).save(proj / "layers" / f"{sid}__0_kid_char.png")
        specs = [{"layer": f"{sid}__0_kid_char", "index": 0, "name": "노란옷 아이",
                  "name_en": "kid", "location": "", "kind": "character", "intent": "",
                  "z": 1, "bbox": [100, 200, 300, 600]}]
        (proj / "layers" / f"{sid}__elements.json").write_text(
            json.dumps(specs, ensure_ascii=False), encoding="utf-8")
        scenes_json["scenes"].append({
            "sceneNumber": n, "sceneId": sid, "title": f"t{n}", "narration": "n",
            "imageRef": f"storyboard/sb_{sid}.png", "duration_estimate_sec": 4})
    (proj / "scenes.json").write_text(json.dumps(scenes_json, ensure_ascii=False), encoding="utf-8")
    return proj


def _manifest(proj):
    res = manifest.build_manifest(proj)
    return json.loads(Path(res["path"]).read_text(encoding="utf-8"))


def test_scene_has_start_and_prefix(tmp_path):
    mf = _manifest(_project(tmp_path))
    assert mf["scenes"][0]["start"] == pytest.approx(0.0)
    assert mf["scenes"][1]["start"] == pytest.approx(4.0)
    assert mf["scenes"][0]["prefix"] == "S01_"
    assert mf["scenes"][1]["prefix"] == "S02_"


def test_every_layer_has_position_and_scale(tmp_path):
    mf = _manifest(_project(tmp_path))
    for sc in mf["scenes"]:
        assert sc["layers"]
        for L in sc["layers"]:
            assert "position" in L and "scale" in L
            assert isinstance(L["position"], list) and len(L["position"]) == 2


def test_layer_ae_names(tmp_path):
    mf = _manifest(_project(tmp_path))
    names = [L["aeName"] for L in mf["scenes"][0]["layers"]]
    assert names[0] == "S01_배경"                 # 배경이 배열 맨 앞
    assert names[1] == "S01_01_노란옷아이"          # 요소는 z 순번 2자리 + 공백 제거한 한글 이름


def test_element_coords_are_comp_space(tmp_path):
    """bbox [100,200,300,600] × f=1.0547 + ox=15 → 중심 x=(100+300)/2*1.0547+15."""
    mf = _manifest(_project(tmp_path))
    el = mf["scenes"][0]["layers"][1]
    f = 1080 / 1024
    ox = (1920 - 1792 * f) / 2
    assert el["position"][0] == pytest.approx(200 * f + ox)
    assert el["position"][1] == pytest.approx(400 * f)
    assert el["foot"][0] == pytest.approx(200 * f + ox)
    assert el["foot"][1] == pytest.approx(600 * f)
    assert el["scale"] == pytest.approx((300 - 100) / 200 * 100 * f)


def test_background_fills_scene_rect(tmp_path):
    """배경판(풀프레임)은 씬 사각형 중앙에 놓이고 f배로 커진다."""
    mf = _manifest(_project(tmp_path))
    bg = mf["scenes"][0]["layers"][0]
    f = 1080 / 1024
    ox = (1920 - 1792 * f) / 2
    assert bg["position"] == pytest.approx([1792 * f / 2 + ox, 540.0])
    assert bg["scale"] == pytest.approx(f * 100)


def test_bg_fill_flag(tmp_path):
    """좌우 여백이 있으면 bgFill이 참."""
    mf = _manifest(_project(tmp_path))
    assert mf["scenes"][0]["bgFill"] is True


def test_fit_block_present(tmp_path):
    mf = _manifest(_project(tmp_path))
    fit = mf["scenes"][0]["fit"]
    assert fit["w"] == 1792 and fit["h"] == 1024
    assert fit["f"] == pytest.approx(1080 / 1024)
    assert fit["ox"] == pytest.approx(15.0, abs=0.1)


def test_image_fit_present(tmp_path):
    """씬 이미지도 같은 변환 — 레이아웃 씬의 배경 이미지가 이것을 쓴다."""
    mf = _manifest(_project(tmp_path))
    imf = mf["scenes"][0]["imageFit"]
    f = 1080 / 1024
    ox = (1920 - 1792 * f) / 2
    assert imf["position"] == pytest.approx([1792 * f / 2 + ox, 540.0])
    assert imf["scale"] == pytest.approx(f * 100)


def test_no_skip_final_key(tmp_path):
    """평면에서는 Final이 유일한 컴프 — 부분 빌드도 같은 컴프에 넣는다."""
    proj = _project(tmp_path)
    manifest.build_manifest(proj)
    res = manifest.build_manifest(proj, only_scene=2)
    mf = json.loads(Path(res["path"]).read_text(encoding="utf-8"))
    assert "skipFinal" not in mf
    assert len(mf["scenes"]) == 1
    assert mf["scenes"][0]["start"] == pytest.approx(4.0)   # 전체 기준 시작 시각 유지


def test_fractional_scene_prefix(tmp_path):
    """삽입 씬 25.25 → S25_25_."""
    proj = _project(tmp_path)
    data = json.loads((proj / "scenes.json").read_text(encoding="utf-8"))
    data["scenes"][1]["sceneNumber"] = 25.25
    (proj / "scenes.json").write_text(json.dumps(data, ensure_ascii=False), encoding="utf-8")
    mf = _manifest(proj)
    assert mf["scenes"][1]["prefix"] == "S25_25_"
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `python3 -m pytest tests/test_manifest_flat.py -v`
Expected: FAIL — `AttributeError: module 'backend.manifest' has no attribute 'fit_transform'`

- [ ] **Step 3: `fit_transform`을 추가한다**

`backend/manifest.py`의 `_img_size` 함수 다음에 넣는다.

```python
def fit_transform(sw, sh, comp_w: int = W, comp_h: int = H):
    """세로 기준 배율과 좌우 중앙 정렬 오프셋 (f, ox).

    f = 컴프높이 / 이미지높이 — 위아래를 자르지 않는다. 폭이 남으면 좌우에 여백이
    생기고(ox 양수), 16:9보다 가로로 길면 좌우로 넘친다(ox 음수).
    크기를 못 읽었거나 비정상이면 (1.0, 0.0)으로 물러선다."""
    try:
        sw = float(sw)
        sh = float(sh)
    except (TypeError, ValueError):
        return (1.0, 0.0)
    if sw <= 0 or sh <= 0:
        return (1.0, 0.0)
    f = comp_h / sh
    return (f, (comp_w - sw * f) / 2)
```

- [ ] **Step 4: `_scene_layers`가 컴프 좌표를 내도록 고친다**

`backend/manifest.py`의 `_scene_layers` 시그니처와 본문을 아래로 교체한다. 기존
docstring의 좌표 설명은 평면 기준으로 바꾼다.

```python
def _scene_layers(proj_dir: Path, layer_rels: list, sid: str = "", scene_width: int | None = None,
                  *, prefix: str = "", f: float = 1.0, ox: float = 0.0,
                  scene_height: int | None = None) -> list:
    """[{name, aeName, path, kind, position, scale, foot?}] — 배경(__bg)을 맨 앞(최하단)으로.

    좌표는 **컴프 공간**이다. layerize 레이어는 요소 크기로 크롭돼 오므로 사이드카 bbox로
    씬 이미지 좌표를 되살린 뒤, 세로 기준 배율 f와 좌우 오프셋 ox를 곱해 컴프 좌표로 굽는다.
    jsx는 이 숫자를 그대로 찍는다 — 계산하지 않는다.

    bbox가 없는 레이어(배경판·레거시 풀프레임)도 좌표를 싣는다. 씬 사각형을 채우도록
    자기 PNG 폭 기준으로 배율을 낸다."""
    from backend import imagegen
    specs = {s.get("layer"): s for s in imagegen.load_element_specs(proj_dir / "layers", sid)} if sid else {}
    bg = [r for r in layer_rels if "__bg" in Path(r).name]
    el = [r for r in layer_rels if "__bg" not in Path(r).name]
    # 프로젝트에서 제거한 요소는 내보내지 않는다. hidden은 패널 미리보기 전용이라 무시한다.
    el = [r for r in el if not (specs.get(Path(r).stem) or {}).get("removed")]
    el.sort(key=lambda r: (specs.get(Path(r).stem, {}).get("z") is None,
                           specs.get(Path(r).stem, {}).get("z") or 0,
                           Path(r).name))
    sw = float(scene_width or W)
    sh = float(scene_height or H)
    scale_factor = 1.0
    if scene_width and bg:
        plate_size = _img_size(proj_dir / bg[0])
        if plate_size and plate_size[0]:
            scale_factor = scene_width / plate_size[0]
    out = []
    idx = 0
    for r in bg + el:
        stem = Path(r).stem
        is_bg = "__bg" in Path(r).name
        # 벡터화한 레이어는 SVG로 내보낸다 — AE에서 연속 래스터화를 켜면 확대해도 깨지지 않는다.
        # 크기 계산은 PNG 기준을 그대로 쓴다. PIL은 SVG를 읽지 못한다.
        svg_rel = str(Path(r).with_suffix(".svg"))
        has_svg = (proj_dir / svg_rel).is_file()
        if is_bg:
            ae_name = prefix + "배경"
        else:
            idx += 1
            nm = (specs.get(stem) or {}).get("name") or stem
            ae_name = "%s%02d_%s" % (prefix, idx, re.sub(r"\s+", "", str(nm)))
        entry = {"name": stem, "aeName": ae_name,
                 "path": _abs(proj_dir, svg_rel if has_svg else r),
                 "kind": "bg" if is_bg else "element"}
        if has_svg:
            entry["vector"] = True
        placed = False
        if not is_bg:
            bbox = (specs.get(stem) or {}).get("bbox")
            if bbox and len(bbox) == 4:
                try:
                    l, t, rr, b = [float(v) * scale_factor for v in bbox]
                except (TypeError, ValueError):
                    l = rr = None      # 비정상(비수치) bbox — bbox 없는 것으로 취급
                if l is not None and (rr - l) > 0:
                    size = _img_size(proj_dir / r)
                    if size and size[0]:
                        entry["position"] = [(l + rr) / 2 * f + ox, (t + b) / 2 * f]
                        entry["scale"] = (rr - l) / size[0] * 100 * f
                        entry["foot"] = [(l + rr) / 2 * f + ox, b * f]
                        placed = True
        if not placed:
            # 풀프레임(배경판·bbox 없는 레거시) — 씬 사각형을 채운다
            size = _img_size(proj_dir / r) or (sw, sh)
            pw = float(size[0] or sw)
            entry["position"] = [sw * f / 2 + ox, sh * f / 2]
            entry["scale"] = sw * f / pw * 100
            if not is_bg:
                foot = _alpha_foot(proj_dir / r)
                if foot:
                    entry["foot"] = [foot[0] * f + ox, foot[1] * f]
        out.append(entry)
    return out
```

파일 맨 위 import에 `re`를 더한다(`import json` 옆).

- [ ] **Step 5: `build_manifest`가 씬 필드를 싣게 한다**

`backend/manifest.py`의 `build_manifest` 안을 고친다.

씬 시작 시각이 필요하므로 루프 앞에 타이밍 표를 만든다. `data = scenes.load_scenes(proj_dir)` 다음 줄에 넣는다.

```python
    starts = {}
    for s_t, start_t, _dur_t in timeline.scene_timings(proj_dir, data):
        starts[_key(s_t.get("sceneNumber"))] = start_t
```

`sw, sh = size if size else (W, H)` 다음 줄에 변환과 접두사를 만든다.

```python
        f, ox = fit_transform(sw, sh)
        prefix = "S%s_" % timeline.comp_num(s.get("sceneNumber"))
```

`layers = ...` 줄을 바꾼다.

```python
        layers = [] if is_layout_scene else _scene_layers(
            proj_dir, s.get("_layers") or [], sid, sw,
            prefix=prefix, f=f, ox=ox, scene_height=sh)
```

`out_scenes.append({...})`에 필드를 더한다. `"duration": dur,` 다음에 넣는다.

```python
            "start": starts.get(_key(s.get("sceneNumber")), 0.0),
            "prefix": prefix,
            "fit": {"f": f, "ox": ox, "w": sw, "h": sh},
            "bgFill": (sw * f) < (W - 1),
            **({"imageFit": {"position": [sw * f / 2 + ox, sh * f / 2],
                             "scale": sw * f / float((_img_size(proj_dir / s["_image"]) or (sw, sh))[0] or sw) * 100}}
               if s.get("_image") else {}),
```

`skipFinal`을 없앤다. 아래 두 줄을 지운다.

```python
    if picked is not None:
        mf["skipFinal"] = True   # 부분 빌드 — Final은 전체 컴프 때만(jsx가 스킵)
```

- [ ] **Step 6: 테스트가 통과하는지 확인한다**

Run: `python3 -m pytest tests/test_manifest_flat.py -v`
Expected: PASS (16건)

- [ ] **Step 7: 기존 테스트를 확인한다**

Run: `python3 -m pytest tests/ -q`
Expected: `skipFinal`이나 씬 컴프 좌표를 단언하던 기존 테스트가 실패할 수 있다.
`grep -rn "skipFinal" tests/ backend/ cep/`로 참조를 찾아, 테스트는 평면 기준으로
고치고(좌표가 컴프 공간이 됐다는 사실을 단언), 코드 참조는 남기지 않는다.

- [ ] **Step 8: 커밋한다**

```bash
git add backend/manifest.py tests/test_manifest_flat.py
git commit -m "feat(manifest): 컴프 좌표 굽기 — 세로 기준 배율, aeName, 씬 start/prefix"
```

---

### Task 2: jsx 평면 조립 코어

**Files:**
- Modify: `cep/com.autokairos.pd/jsx/build_scene.jsx` (`addLayerObj` 322-345, `addBobNull` 347-363, `applyMoves` 365-430, `applyCamera` 431-452, 빌드 루프 495-560)
- Test: `tests/test_flat_jsx.py` (신규)

**Interfaces:**
- Consumes: Task 1이 내보내는 매니페스트 — 씬의 `start`·`duration`·`prefix`·`bgFill`·`fit`·`imageFit`, 레이어의 `aeName`·`position`·`scale`·`foot`·`vector`·`moves`
- Produces: jsx 함수 `akFindOrMakeComp(proj, name, W, H, fps, dur)`, `akRemoveSceneGroup(comp, prefix)`, `akGroupAnchor(comp, scenes, idx)`, `buildSceneGroup(proj, comp, s, W, H, log)`

**배경:** 지금 빌드 루프는 씬마다 `proj.items.addComp(...)`로 컴프를 만들고 마지막에
Final을 만들어 씬 컴프를 얹는다. 이것을 통째로 바꾼다. jsx는 ExtendScript라 정적
문자열 검사로만 테스트한다(`tests/test_panel_structure.py`의 기존 관례).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_flat_jsx.py`를 만든다.

```python
from pathlib import Path

PANEL = Path(__file__).resolve().parents[1] / "cep" / "com.autokairos.pd"
JSX = PANEL / "jsx" / "build_scene.jsx"


def _src():
    return JSX.read_text(encoding="utf-8")


def test_no_per_scene_comp():
    """씬마다 컴프를 만들지 않는다 — 평면 컴프 하나."""
    src = _src()
    assert "proj.items.addComp" in src            # Final은 만든다
    assert src.count("proj.items.addComp") == 1   # 그 한 번뿐


def test_flat_helpers_exist():
    src = _src()
    for fn in ("function akFindOrMakeComp", "function akRemoveSceneGroup",
               "function akGroupAnchor", "function buildSceneGroup"):
        assert fn in src


def test_guide_null_created_and_named():
    src = _src()
    assert 's.prefix + "가이드"' in src
    assert "addNull" in src


def test_camera_targets_guide():
    """카메라는 가이드 널을 잡는다 — 씬 컴프 레이어가 아니다."""
    src = _src()
    assert "applyCamera(guide" in src
    assert "fc.layers.add(comps[" not in src


def test_layers_use_baked_coords_only():
    """jsx가 채움 스케일을 계산하지 않는다 — 좌표는 매니페스트가 굽는다."""
    src = _src()
    assert "Math.max(W / sw, H / sh)" not in src
    assert "layer.position" in src and "layer.scale" in src


def test_scene_time_applied():
    src = _src()
    assert "inPoint" in src and "outPoint" in src
    assert "startTime" in src            # 오디오


def test_bob_null_stays_in_group():
    """까딱까딱 널은 이름에 접두사가 붙고 레이어 바로 아래로 옮겨진다."""
    src = _src()
    assert "moveAfter" in src
    assert '"_피벗"' in src


def test_target_comp_is_final():
    src = _src()
    assert '"Final"' in src
    assert "app.project.activeItem" not in src


def test_no_skip_final():
    assert "skipFinal" not in _src()


def test_es5_only():
    src = _src()
    assert "=>" not in src
    assert "const " not in src and "let " not in src
    assert "`" not in src
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `python3 -m pytest tests/test_flat_jsx.py -v`
Expected: FAIL — 헬퍼 함수들이 없고 씬 컴프 생성 코드가 남아 있다.

- [ ] **Step 3: 헬퍼를 추가한다**

`cep/com.autokairos.pd/jsx/build_scene.jsx`에서 `addLayerObj` 함수 **앞에** 넣는다.

```javascript
    // 평면 컴프 — 컴프는 Final 하나. 씬은 S{번호}_ 접두사 레이어 그룹으로 구분한다.
    function akFindOrMakeComp(proj, name, W, H, fps, dur) {
        for (var i = 1; i <= proj.numItems; i++) {
            var it = proj.item(i);
            if (it instanceof CompItem && it.name === name) {
                if (dur > it.duration) { it.duration = dur; }
                return it;
            }
        }
        return proj.items.addComp(name, W, H, 1.0, Math.max(dur, 1), fps);
    }

    // 이 씬의 레이어를 전부 지운다(재빌드는 지우고 다시 넣는다).
    function akRemoveSceneGroup(comp, prefix) {
        var n = 0;
        for (var i = comp.numLayers; i >= 1; i--) {
            if (comp.layer(i).name.indexOf(prefix) === 0) { comp.layer(i).remove(); n++; }
        }
        return n;
    }

    // 다음 씬 그룹의 최상단 레이어 — 새 그룹을 그 위에 놓아 씬 번호 순서를 지킨다.
    function akGroupAnchor(comp, scenes, idx) {
        for (var j = idx + 1; j < scenes.length; j++) {
            var pf = scenes[j].prefix;
            if (!pf) { continue; }
            for (var i = 1; i <= comp.numLayers; i++) {
                if (comp.layer(i).name.indexOf(pf) === 0) { return comp.layer(i); }
            }
        }
        return null;
    }
```

- [ ] **Step 4: `addLayerObj`가 구운 좌표만 쓰게 한다**

`addLayerObj` 함수 본문에서 좌표 분기를 없애고 매니페스트 값을 그대로 쓴다.

기존 `if (layer.position) { ... } else { ... }` 블록 전체를 아래로 바꾼다.

```javascript
        // 좌표는 매니페스트가 컴프 공간으로 구워서 준다 — 여기서 계산하지 않는다.
        var lp = layer.position || [W / 2, H / 2];
        var ls = (layer.scale != null) ? layer.scale : 100;
        il.property("Position").setValue([lp[0], lp[1]]);
        il.property("Scale").setValue([ls, ls]);
```

- [ ] **Step 5: `addBobNull`이 그룹 안에 머물게 한다**

`addBobNull` 함수를 아래로 교체한다. 널이 컴프 맨 위로 떠오르지 않게 레이어 바로
아래로 옮기고, 원래 부모를 널에 물려준다(가이드 널로 이어진다).

```javascript
    function addBobNull(comp, il, layer, t0, amt, tEnd) {
        var prevParent = il.parent;
        il.parent = null;
        var nl = comp.layers.addNull();
        nl.name = (layer.aeName || layer.name || "el") + "_피벗";
        nl.property("Position").setValue([layer.foot[0], layer.foot[1]]);
        nl.inPoint = il.inPoint;
        nl.outPoint = il.outPoint;
        il.parent = nl;                                   // AE가 월드 변환 보존하며 페어런팅
        if (prevParent) { nl.parent = prevParent; }
        nl.moveAfter(il);                                 // 씬 그룹 안에 머무르게
        var sp = nl.property("Scale");
        var half = 0.6;                                   // 반주기 0.6s
        sp.setValueAtTime(t0, [100, 100]);
        sp.setValueAtTime(Math.min(tEnd, t0 + half), [100, 100 + amt]);
        try {                                             // easy ease 양 키
            var ez = new KeyframeEase(0, 33.34);
            sp.setTemporalEaseAtKey(1, [ez, ez], [ez, ez]);
            sp.setTemporalEaseAtKey(2, [ez, ez], [ez, ez]);
        } catch (e) { }
        try { sp.expression = 'loopOut("pingpong")'; } catch (e) { }
        return nl;
    }
```

- [ ] **Step 6: `applyMoves`가 절대 시각을 쓰게 한다**

`applyMoves` 시그니처와 시각 계산만 바꾼다. 모션 종류별 분기는 건드리지 않는다.

시그니처를 바꾼다.

```javascript
    function applyMoves(comp, il, layer, sceneStart, sceneDur, cw, ch) {
```

시각 두 줄을 바꾼다.

```javascript
            var t0 = sceneStart + Math.max(0, mv.start || 0);
            var t1 = Math.min(sceneStart + sceneDur, t0 + (mv.duration || 0.5));
```

`bob` 분기의 `addBobNull` 호출 인자를 바꾼다.

```javascript
                        addBobNull(comp, il, layer, t0, (amt && amt <= 5 ? amt : 1), sceneStart + sceneDur);
```

- [ ] **Step 7: 씬 그룹 빌더를 추가한다**

`applyCamera` 함수 **다음에** 넣는다.

```javascript
    // 씬 하나를 평면 컴프에 놓는다. 쌓임은 위에서 아래로 요소 → 배경 → 판 → 가이드 널.
    // comp.layers.add는 맨 위에 넣으므로 가이드부터 거꾸로 추가하면 그 순서가 나온다.
    function buildSceneGroup(proj, comp, s, W, H, log) {
        var pf = s.prefix || "S00_";
        var t0 = s.start || 0;
        var dur = s.duration || 5;
        var t1 = t0 + dur;

        var guide = comp.layers.addNull();
        guide.name = pf + "가이드";
        guide.property("Position").setValue([W / 2, H / 2]);
        guide.inPoint = t0; guide.outPoint = t1;

        if (s.bgFill) {                                   // 좌우 여백 — 테마 배경색으로
            var c = TK.colors.bgRgb;
            var fill = comp.layers.addSolid([c[0] / 255, c[1] / 255, c[2] / 255], pf + "판", W, H, 1.0);
            fill.inPoint = t0; fill.outPoint = t1;
            fill.parent = guide;
        }

        if (s.layers && s.layers.length) {
            for (var li = 0; li < s.layers.length; li++) {
                var lay = s.layers[li];
                var il = addLayerObj(proj, comp, lay, W, H);
                if (!il) { log.push(pf + "레이어 누락 " + (lay.aeName || lay.name)); continue; }
                il.name = lay.aeName || lay.name;
                il.inPoint = t0; il.outPoint = t1;
                il.parent = guide;
                if (lay.moves) { applyMoves(comp, il, lay, t0, dur, W, H); }
                var top = il;                             // 까딱까딱 널이 끼면 그 널이 가이드의 자식
                while (top.parent && top.parent !== guide) { top = top.parent; }
                if (top !== guide && !top.parent) { top.parent = guide; }
            }
        } else if (s.image) {
            var one = addLayerObj(proj, comp, {
                path: s.image,
                position: (s.imageFit || {}).position,
                scale: (s.imageFit || {}).scale
            }, W, H);
            if (one) {
                one.name = pf + "이미지";
                one.inPoint = t0; one.outPoint = t1;
                one.parent = guide;
            } else { log.push(pf + "image 누락"); }
        }

        if (s.audio) {
            var aF = new File(s.audio);
            if (aF.exists) {
                var al = comp.layers.add(proj.importFile(new ImportOptions(aF)));
                al.name = pf + "음성";
                al.startTime = t0;                        // 이것이 없으면 0초부터 재생된다
                al.inPoint = t0; al.outPoint = t1;
            }
        }

        if (s.camera) { applyCamera(guide, s.camera, t0, dur, 100, W, H); }
        return guide;
    }
```

- [ ] **Step 8: 빌드 루프를 평면으로 바꾼다**

`var comps = [], totalDur = 0, log = [];` 부터 `return "OK: 씬 컴프 ...";` 까지의
빌드 루프와 Final 조립부(약 495-560줄)를 **전부** 아래로 교체한다.

```javascript
        var log = [];
        var endT = 0;
        for (var ei = 0; ei < scenes.length; ei++) {
            var se = (scenes[ei].start || 0) + (scenes[ei].duration || 5);
            if (se > endT) { endT = se; }
        }
        var comp = akFindOrMakeComp(proj, "Final", W, H, FPS, endT);

        for (var i = 0; i < scenes.length; i++) {
            var s = scenes[i];
            akRemoveSceneGroup(comp, s.prefix);           // 재빌드 — 지우고 다시 넣는다
            var before = comp.numLayers;
            try { buildSceneGroup(proj, comp, s, W, H, log); }
            catch (eB) { log.push((s.prefix || "") + "빌드 실패 " + eB.toString()); continue; }
            var made = comp.numLayers - before;
            var group = [];                               // 인덱스는 옮기는 즉시 밀리므로 참조를 먼저 모은다
            for (var k = 1; k <= made && k <= comp.numLayers; k++) { group.push(comp.layer(k)); }
            var anchor = akGroupAnchor(comp, scenes, i);  // 다음 씬 그룹 위로 옮긴다
            if (anchor) {
                // 위에서부터 차례로 anchor 바로 위에 놓으면 그룹 안 순서가 그대로 유지된다
                for (var g = 0; g < group.length; g++) { group[g].moveBefore(anchor); }
            }
        }
        comp.openInViewer();
        app.endUndoGroup();

        return "OK: 씬 " + scenes.length + "개 → Final(" + endT + "s)" +
               (log.length ? " | " + log.join(", ") : "") +
               (FONT_WARN.length ? " | 폰트: " + FONT_WARN.join(", ") : "");
```

- [ ] **Step 9: 검증한다**

Run: `python3 -m pytest tests/test_flat_jsx.py -v`
Expected: PASS (10건)

Run: `grep -nE "=>|\blet\b|\bconst\b" cep/com.autokairos.pd/jsx/build_scene.jsx`
Expected: 출력 없음

Run: `python3 -m pytest tests/ -q`
Expected: 실패 없음. 씬 컴프를 전제하던 기존 jsx 단언(`tests/test_panel_structure.py`)이
깨지면 평면 구조를 검사하도록 갱신한다 — 검증을 약화시키지 말고 새 구조를 단언하게 한다.

- [ ] **Step 10: 커밋한다**

```bash
git add cep/com.autokairos.pd/jsx/build_scene.jsx tests/test_flat_jsx.py
git commit -m "feat(jsx): 평면 컴프 조립 — 씬 컴프 폐지, 가이드 널이 씬 손잡이"
```

---

### Task 3: 레이아웃·지도 씬을 평면으로

**Files:**
- Modify: `cep/com.autokairos.pd/jsx/build_scene.jsx` (`renderLayout` 295-320, `buildSceneGroup`)
- Test: `tests/test_flat_jsx.py` (Task 2에서 만든 파일에 추가)

**Interfaces:**
- Consumes: Task 2의 `buildSceneGroup(proj, comp, s, W, H, log)`, 매니페스트의 `imageFit`·`fit`
- Produces: jsx 함수 `akTagGroup(comp, fromIndex, prefix, guide)` — 방금 만들어진 레이어 구간에 접두사를 붙이고 가이드에 페어런팅

**배경:** `renderLayout`은 지금 씬 컴프 크기로 그리고 `S = W / 1920`으로 배율을 잡는다.
평면에서는 `W=1920, H=1080`이라 `S=1`이 되어 그대로 맞는다. 문제는 그린 레이어를
가이드 널에 묶고 접두사를 붙이는 것이다 — `renderLayout`은 만든 레이어를 돌려주지
않으므로, 그리기 전후의 `comp.numLayers`로 구간을 잡는다.

`renderLayout` 안의 `addBgSolid(comp, W, H, c.bgRgb)`는 남긴다 — 레이아웃 씬의 바탕이다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_flat_jsx.py` 끝에 붙인다.

```python
def test_layout_group_tagged():
    """레이아웃 씬이 만든 셰이프·텍스트도 접두사가 붙고 가이드에 묶인다."""
    src = _src()
    assert "function akTagGroup" in src
    assert "akTagGroup(comp" in src


def test_layout_image_uses_image_fit():
    """레이아웃 씬의 배경 이미지도 구운 좌표를 쓴다."""
    src = _src()
    assert "s.imageFit" in src
    assert "Math.max(W / isw, H / ish)" not in src


def test_map_overlay_still_called():
    src = _src()
    assert "renderMapOverlay" in src
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `python3 -m pytest tests/test_flat_jsx.py -v`
Expected: FAIL — `akTagGroup`이 없고 `renderLayout`이 아직 채움 스케일을 계산한다.

- [ ] **Step 3: `akTagGroup`을 추가한다**

Task 2에서 넣은 `akGroupAnchor` 다음에 넣는다.

```javascript
    // 방금 만들어진 레이어 구간(맨 위 ~ fromIndex)에 접두사를 붙이고 가이드에 묶는다.
    // renderLayout/renderMapOverlay가 만든 레이어를 돌려주지 않으므로 개수로 구간을 잡는다.
    function akTagGroup(comp, madeCount, prefix, guide, t0, t1) {
        for (var i = 1; i <= madeCount && i <= comp.numLayers; i++) {
            var lay = comp.layer(i);
            if (lay === guide) { continue; }
            if (lay.name.indexOf(prefix) !== 0) { lay.name = prefix + lay.name; }
            lay.inPoint = t0; lay.outPoint = t1;
            if (!lay.parent) { lay.parent = guide; }
        }
    }
```

- [ ] **Step 4: `renderLayout`의 이미지 배치를 구운 좌표로 바꾼다**

`renderLayout` 안의 `if (s.image) { ... }` 블록에서 크기 계산을 없앤다.

기존:
```javascript
                    var isw = imgL.source.width, ish = imgL.source.height;
                    imgL.property("Anchor Point").setValue([isw / 2, ish / 2]);
                    imgL.property("Position").setValue([W / 2, H / 2]);
                    var ifs = Math.max(W / isw, H / ish) * 100;
                    imgL.property("Scale").setValue([ifs, ifs]);
```

교체:
```javascript
                    // 좌표는 매니페스트가 구워서 준다 — 세로 기준 배율이라 위아래가 안 잘린다.
                    var ifit = s.imageFit || {};
                    var ipos = ifit.position || [W / 2, H / 2];
                    var isc = (ifit.scale != null) ? ifit.scale : 100;
                    imgL.property("Anchor Point").setValue([imgL.source.width / 2, imgL.source.height / 2]);
                    imgL.property("Position").setValue([ipos[0], ipos[1]]);
                    imgL.property("Scale").setValue([isc, isc]);
```

- [ ] **Step 5: `buildSceneGroup`이 레이아웃·지도를 처리하게 한다**

Task 2에서 만든 `buildSceneGroup`에서, 가이드 널과 여백 판을 만든 **다음**이자
`if (s.layers && s.layers.length)` **앞**에 넣는다.

```javascript
        var isLayoutScene = s.layout && s.layout !== "cinematic";
        if (isLayoutScene) {
            var beforeL = comp.numLayers;
            try { renderLayout(proj, comp, s, W, H); }
            catch (eL) { log.push(pf + "레이아웃 렌더 실패 " + eL.toString()); }
            akTagGroup(comp, comp.numLayers - beforeL, pf, guide, t0, t1);
        }
```

그리고 오디오 처리 **앞**에 지도 오버레이를 넣는다.

```javascript
        if (s.mapGeo) {
            var beforeM = comp.numLayers;
            try { renderMapOverlay(comp, s.mapGeo, W, H, dur); }
            catch (eMap) { log.push(pf + "지도 오버레이 실패 " + eMap.toString()); }
            akTagGroup(comp, comp.numLayers - beforeM, pf, guide, t0, t1);
        }
```

레이아웃 씬은 `s.layers`가 비어 있고 `s.image`도 레이아웃 렌더가 이미 그렸으므로,
`else if (s.image)` 분기가 중복으로 그리지 않도록 조건을 바꾼다.

기존:
```javascript
        } else if (s.image) {
```

교체:
```javascript
        } else if (s.image && !isLayoutScene) {
```

- [ ] **Step 6: 검증한다**

Run: `python3 -m pytest tests/test_flat_jsx.py -v`
Expected: PASS (13건)

Run: `grep -nE "=>|\blet\b|\bconst\b" cep/com.autokairos.pd/jsx/build_scene.jsx`
Expected: 출력 없음

Run: `python3 -m pytest tests/ -q`
Expected: 실패 없음

- [ ] **Step 7: 커밋한다**

```bash
git add cep/com.autokairos.pd/jsx/build_scene.jsx tests/test_flat_jsx.py
git commit -m "feat(jsx): 레이아웃·지도 씬 평면화 — 접두사 태깅 + 가이드 페어런팅"
```

---

### Task 4: 씬 단위 재배치 — `place_on_timeline` 제거와 패널 배선

**Files:**
- Delete: `cep/com.autokairos.pd/jsx/place_on_timeline.jsx`
- Modify: `cep/com.autokairos.pd/index.html` (jsx 로드 목록에서 제거)
- Modify: `cep/com.autokairos.pd/js/storyboard.js` (⤓ 버튼 핸들러)
- Modify: `backend/router.py` (타임라인 배치 엔드포인트가 있으면 정리)
- Test: `tests/test_flat_jsx.py`(추가), `tests/test_panel_structure.py`(갱신)

**Interfaces:**
- Consumes: Task 2의 `akRemoveSceneGroup(comp, prefix)`·`buildSceneGroup(...)` — 씬 재빌드는 이미 빌드 경로가 멱등하게 한다
- Produces: 없음(제거와 배선)

**배경:** `place_on_timeline.jsx`의 `akPlaceOnTimeline(planJson)`은 **씬 컴프를**
활성 컴프에 얹는다. 평면에는 씬 컴프가 없으므로 이 파일은 성립하지 않는다. 패널의
⤓(이 씬을 현재 타임라인에 배치) 버튼은 그 씬 하나만 빌드하는 경로를 부르면 된다 —
빌드가 `S{번호}_` 그룹을 지우고 다시 넣으므로 결과가 같다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_flat_jsx.py` 끝에 붙인다.

```python
def test_place_on_timeline_removed():
    """씬 컴프를 얹던 스크립트는 평면에서 성립하지 않는다."""
    assert not (PANEL / "jsx" / "place_on_timeline.jsx").exists()
    html = (PANEL / "index.html").read_text(encoding="utf-8")
    assert "place_on_timeline.jsx" not in html
    js = (PANEL / "js" / "storyboard.js").read_text(encoding="utf-8")
    assert "akPlaceOnTimeline" not in js


def test_scene_build_is_idempotent():
    """같은 씬을 다시 빌드하면 기존 그룹을 지우고 다시 넣는다."""
    src = _src()
    assert "akRemoveSceneGroup(comp, s.prefix)" in src
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `python3 -m pytest tests/test_flat_jsx.py -v`
Expected: FAIL — `place_on_timeline.jsx`가 아직 있다.

- [ ] **Step 3: 남은 참조를 찾는다**

Run: `grep -rn "place_on_timeline\|akPlaceOnTimeline\|/api/timeline" cep/ backend/ tests/`

`/api/timeline/plan` 엔드포인트를 부르는 곳이 패널의 `exportToTimeline` 하나뿐이면
그 엔드포인트도 `backend/router.py`에서 지운다. `backend/timeline.py`의
`build_plan` 함수 자체는 매니페스트가 쓰므로 **지우지 않는다.**

- [ ] **Step 4: 파일을 지우고 배선을 고친다**

```bash
git rm cep/com.autokairos.pd/jsx/place_on_timeline.jsx
```

`cep/com.autokairos.pd/index.html`에 `place_on_timeline.jsx`를 언급하는 줄이 있으면
지운다.

`cep/com.autokairos.pd/js/storyboard.js`의 `exportToTimeline` 함수(약 1151-1176줄)
전체를 아래로 교체한다. `main.js`의 `_assemble(scope, statusFn)`이 이미 매니페스트
빌드 + `akBuildScene` 실행을 하고, 빌드가 `S{번호}_` 그룹을 지우고 다시 넣으므로
"배치"와 "재빌드"가 같은 동작이 된다. 호출부(`exportToTimeline(n)`,
`exportAllToTimeline()`)는 그대로 두고 속만 바꾼다.

```javascript
/* ⤓ — 이 씬을 타임라인에 다시 놓는다. 평면 컴프라 씬 빌드가 곧 재배치다:
   빌드가 S{번호}_ 로 시작하는 레이어를 지우고 같은 시각 구간에 다시 넣는다.
   sceneNumber=null이면 전체. */
function exportToTimeline(sceneNumber) {
  var say = sceneNumber == null
    ? function (m) { var e = $("aeresult"); if (e) e.textContent = m; }
    : function (m) { _rowStatus(sceneNumber, m); };
  if (!SELECTED_PROJECT) { say("프로젝트를 먼저 선택하세요."); return; }
  if (typeof _assemble !== "function") { say("빌드 함수를 찾지 못했습니다."); return; }
  return _assemble(sceneNumber == null ? null : parseInt(sceneNumber, 10), say);
}
```

`exportAllToTimeline`은 그대로 둔다 — `exportToTimeline(null)`을 부른다.

- [ ] **Step 5: `tests/test_panel_structure.py`를 갱신한다**

Run: `grep -n "place_on_timeline\|akPlaceOnTimeline" tests/test_panel_structure.py`

단언이 있으면 지우지 말고 **평면 구조를 검사하도록 바꾼다** — 예를 들어
`build_scene.jsx`에 `akRemoveSceneGroup`이 있는지, `place_on_timeline.jsx`가 없는지를
단언한다. 검증을 약화시키지 않는다.

- [ ] **Step 6: 검증한다**

Run: `python3 -m pytest tests/ -q`
Expected: 실패 없음

Run: `node --check cep/com.autokairos.pd/js/storyboard.js`
Expected: 출력 없음

Run: `grep -rn "place_on_timeline\|akPlaceOnTimeline" cep/ backend/ tests/`
Expected: 출력 없음

- [ ] **Step 7: 커밋한다**

```bash
git add -A cep backend tests
git commit -m "refactor(panel): place_on_timeline 제거 — 씬 재배치는 씬 빌드가 대신한다"
```

---

## 사람이 확인해야 하는 것

자동 검증이 되지 않는다. 구현이 끝난 뒤 테스터 PC에서 봐야 한다.

1. **AE에서의 실제 배치.** 이 맥에는 After Effects가 설치돼 있지 않다. 씬이 제자리에 놓이는지, 위아래가 잘리지 않는지, 좌우 여백 색이 배경과 이어지는지 확인해야 한다.
2. **씬 그룹 교체.** 한 씬을 다시 빌드할 때 다른 씬의 레이어를 건드리지 않는지, 씬 번호 순서가 유지되는지.
3. **까딱까딱 널의 위치.** 발밑 피벗이 구운 좌표에서도 발밑에 오는지.
4. **카메라.** 가이드 널의 줌·팬이 씬 전체를 움직이는지, 줌아웃에서 여백이 드러나지 않는지.
5. **레이아웃 씬.** 차트·지도의 셰이프·텍스트가 접두사를 얻고 가이드에 묶여 접히는지.
